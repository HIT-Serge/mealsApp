expo / download expo go app on your phone / download android studio for windows - more options - device manager - add an emulator but it must be from the PlayStore

npm install -g expo-cli
npx expo install @types/expo @types/react @types/react-native @types/react-test-renderer @types/jest
npx expo start -c
npm install @react-navigation/native
npx expo install react-native-screens react-native-safe-area-context / npm install maybe
npm install @react-navigation/native-stack / npx epo install maybe
npm install typescript --save-dev  
npx create-expo-app {name} / npx create-react-native-app -t with-splash-screen
npx create-expo-app --template
npx expo install --fix
npm install -g react-devtools  
npx expo install expo-linear-gradient
npx expo install expo-font
npx expo install expo-font @expo-google-fonts/inter
npx expo install expo-app-loading' // deprecated // npx expo install expo-splash-screen
npm install -g npm@latest latest npm version
npm outdated checks dependencies
npm install -g npm-check-updates installs all new versions of dependencies
ncu -u updates package.json file
npm uninstall @types/react-native uninstalls specific dependency

environment variables - new - name: Path value: %USERPROFILE%\AppData\Roaming\npm



Problem with: 
Fonts and splash screen did not work 
ios and android tsx files for platform specific styling did not work
No colors visible in vscode when hex-codes are showed

Bij een new project vervang ik de package.json en dan updaten npm expo install
Depending on your package manager preference, you can do this using npm or yarn. Use the following command: npm install expo@^48.0. 0. Upgrade all dependencies: Guarantee that all your project's dependencies align with the new Expo SDK version requirements by using the following command: npx expo install --fix





